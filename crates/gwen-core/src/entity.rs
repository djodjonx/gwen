//! Entity management
//!
//! Handles entity spawning, deletion, and tracking.

/// Represents a unique entity ID
pub type EntityId = u32;

/// Entity manager - handles entity lifecycle
pub struct EntityManager {
    // To be implemented in TASK_1.2
}

impl EntityManager {
    /// Create a new entity manager
    pub fn new(max_entities: u32) -> Self {
        EntityManager {}
    }
}

