/**
 * GWEN Engine - Main class
 *
 * Wraps WASM core and provides TypeScript API
 */
export class Engine {
  // To be implemented in Phase 1 tasks
  constructor(config?: { maxEntities?: number }) {
    // Implementation will load WASM + initialize
  }
}

/**
 * Get the global engine instance
 */
export function useEngine(): Engine {
  // To be implemented
  throw new Error('Not implemented yet');
}

